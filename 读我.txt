右键点击 flipclock.scr，选择“安装”。接下来就可以设置屏保为 flipclock 了。
如果想修改 12/24 小时制，可以右键这个目录下面的 flipclock.conf 用记事本打开，并按照里面的说明修改保存。如果找不到这个文件，需要首先运行一次屏保，程序会自动创建这个文件。
如果想替换字体，可以直接将其它字体重命名为 flipclock.ttf 并放置在这个目录下面。
不建议单独提取 flipclock.scr 放到别处，程序运行需要依赖这个目录下的其他文件。
如果下载了更新版本的程序并想要保持配置，请将 flipclock.conf 文件复制粘贴到新版本程序的目录。
